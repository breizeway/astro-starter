---
import type { CollectionEntry } from 'astro:content';
interface Props {
  aboutPage: CollectionEntry<'aboutPage'>;
}

// `post` will match your 'blog' collection schema type
const { aboutPage } = Astro.props;
---

<h2>{aboutPage.title}</h2>