---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"section"> {}

const props = Astro.props;
---

<section {...props} class:list={["page-section", props.class]}></section>

<style is:global>
  .page-section {
    @apply w-full;
  }
</style>
